{
  "name": "thomas-schulze-tests",
  "version": "1.0.0",
  "description": "Playwright test suite for the Thomas Schulze IT Solutions static site",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
